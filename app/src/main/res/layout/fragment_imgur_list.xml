<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/backgroundColorMain"
    android:clipChildren="false"
    android:clipToPadding="false"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context=".ui.fragments.ImgurListFragment">

    <android.support.constraint.ConstraintLayout
        android:id="@+id/no_images_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="center"
        tools:visibility="visible">

        <ImageView
            android:id="@+id/no_images_logo"
            android:layout_width="@dimen/img_logo_size_big"
            android:layout_height="@dimen/img_logo_size_big"
            android:adjustViewBounds="true"
            android:contentDescription="@string/logo_a11y"
            android:scaleType="fitCenter"
            android:src="@drawable/owl_24dp_color_nobg"
            app:layout_constraintBottom_toTopOf="@+id/upload_image_button"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintVertical_chainStyle="packed" />

        <Button
            android:id="@+id/upload_image_button"
            android:layout_width="100dp"
            android:layout_height="40dp"
            android:layout_marginTop="@dimen/button_margin_empty_list"
            android:background="@drawable/btn_background"
            android:elevation="2dp"
            android:fontFamily="sans-serif-condensed"
            android:text="@string/new_img_upload"
            android:textColor="@color/primaryTextColor"
            android:textStyle="bold"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/no_images_logo" />
    </android.support.constraint.ConstraintLayout>


    <android.support.v7.widget.RecyclerView
        android:id="@+id/imgur_recycler"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="center"
        android:clipChildren="false"
        android:clipToPadding="false"
        android:padding="@dimen/margin_4dp"
        android:visibility="invisible"
        app:layoutManager="android.support.v7.widget.StaggeredGridLayoutManager"
        app:spanCount="2"
        tools:listitem="@layout/imgur_list_vh_bar_bottom"
        tools:visibility="gone" />

</android.support.design.widget.CoordinatorLayout>